import { StyleSheet } from 'react-native';
import { Alignment } from '../../Theme';

const styles = StyleSheet.create({
  dropdownBtn: {
    borderRadius: 0,
    ...Alignment.MTxSmall,
  },
});

export { styles };
